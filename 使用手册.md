# PDF 编辑器操作手册

基于 Inkoro 扩展的全功能 Web PDF 编辑器，运行在浏览器中，支持文字编辑、注释标注、页面操作、合并拆分、表单填写、加密解密等功能。

---

## 一、启动与访问

### 开发模式
```bash
cd E:\PDF editior\Inkoro
bun run dev
```
浏览器打开 **http://localhost:3000**

### 生产模式（构建后预览）
```bash
bun run build      # 构建
bun run preview    # 本地预览（端口可能为 3000/3001/3002）
```

### 系统要求
- 浏览器：Chrome / Edge / Firefox 等现代浏览器
- 首次使用需安装 bun（`powershell -c "irm bun.sh/install.ps1 | iex"`）

---

## 二、界面布局

```
┌──────────────────────────────────────────────────────────┐
│  侧边栏: 页面/图层  |      PDF 页面渲染区域                │
│  ────────────────  |                                      │
│  [1] [2] [3] 缩略图 |                                      │
│  (可拖拽排序/删除)   |                                      │
│  ────────────────  |                                      │
│  [表单填写]         |                                      │
│  [加密/解密]        |                                      │
│  [导出]            |                                      │
├──────────────────────────────────────────────────────────┤
│  底部工具栏: 选择|文字|图片|签名|形状|复制|粘贴|◀ 页数 ▶|− 缩放 +| 合并/拆分|导出 │
└──────────────────────────────────────────────────────────┘
```

- **左上角菜单**（☰）：文件（下载/新会话/重置会话）、编辑（撤销/重做/复制/粘贴）、视图（缩放/主题）、关于
- **右侧属性面板**：选中元素后自动弹出，可调颜色、字体、字号、边框等

---

## 三、基本操作

### 1. 打开 PDF
- 启动后自动弹出"上传 PDF"对话框
- **拖拽** PDF 文件到虚线区域，或**点击**选择文件
- 仅支持 `.pdf` 格式

### 2. 翻页与缩放
- 底部工具栏 `◀ 3 / 10 ▶` 翻页，或点击左侧缩略图跳转
- `−` `+` 按钮缩放，范围 50%~300%
- 按住 `空格` 拖拽可平移画布

### 3. 撤销 / 重做
- 菜单"编辑"→ 撤销 / 重做，或快捷键 `Ctrl+Z` / `Ctrl+Y`
- 最多保留 50 步历史

### 4. 会话保存
- 编辑内容自动保存在浏览器 localStorage（刷新不丢失，PDF ≤3MB）
- "新会话"：清空一切；"重置会话"：保留 PDF，清空编辑

---

## 四、添加与编辑内容

### 添加文字
1. 点击底部工具栏 **"T" 文字** 图标
2. 在页面任意位置**单击**，出现文字框
3. 输入文字（支持中文）
4. 按 `Enter` 确认，或点击其他位置

### 编辑文字属性
1. 点击 **"选择"**（箭头）图标
2. 单击文字框选中它（右侧弹出属性面板）
3. 可修改：内容、字体、字号、粗体/斜体/下划线、对齐、文字颜色、背景色
4. 按住文字框可拖动位置；拖拽边角可调整大小

### 插入图片
1. 点击 **图片** 图标
2. 拖拽图片到对话框，或粘贴图片 URL
3. 图片插入页面后可用"选择"工具拖动、缩放、旋转

### 添加签名
1. 点击 **签名** 图标
2. 选择"手写"（鼠标/触屏绘制）或"上传"（签名图片）
3. 点击"插入签名"添加到页面

### 绘制形状
点击 **形状** 图标，从下拉菜单选择：
- 矩形、圆形、直线、箭头
- 选中后可在属性面板调整：填充色、描边色、描边宽度、圆角、透明度、线型（实线/虚线/点线）、箭头端点、曲线

### 复制 / 粘贴
- 选中元素后点"复制"（`Ctrl+C`），点"粘贴"（`Ctrl+V`）
- 支持跨页面粘贴；也可从系统剪贴板粘贴图片/文本

### 删除元素
- 选中后按 `Delete` 键，或属性面板底部"删除图层"

---

## 五、页面操作

### 旋转页面
- 在左侧**缩略图**右下角点 **⟳ 旋转** 按钮，每点一次顺时针旋转 90°
- 缩略图下方会显示角度（如 `第 2 页 · 90°`）

### 删除页面
- 打开左侧"页面"标签 → 缩略图右上角 **✕**

### 重排页面
- 左侧"图层"标签可拖拽排序**图层**（同页元素的前后顺序）
- 页面缩略图本身在侧边栏按顺序显示，当前实现中页面重排需通过图层列表管理元素层级

---

## 六、PDF 合并

1. 点击底部工具栏 **合并/拆分** 图标 → 选"合并 PDF"
2. 选择多个 PDF 文件（拖拽或点击，可多选）
3. 文件列表中**拖拽调整合并顺序**，点 ✕ 移除
4. 点 **"合并并下载"**，生成 `merged.pdf`

> 提示：合并功能独立于当前编辑文档，直接输出新文件。

---

## 七、PDF 拆分

1. 点击 **合并/拆分** 图标 → 选"拆分 PDF"
2. 两种模式：
   - **按范围拆分**：输入如 `1-5, 8, 10-15`（连续段自动合并为一个文件）
   - **每页一个文件**：每页生成独立 PDF
3. 点 **"拆分并下载"**，自动下载多个文件（`拆分_第1部分.pdf` 等）

---

## 八、表单填写

> 适用于含 AcroForm 字段的 PDF（如申请表、合同）。

1. 点击左侧 **"表单填写"** 按钮，右侧弹出表单面板
2. 自动列出所有字段：
   - 文本框 → 直接输入
   - 复选框 → 开关切换
   - 单选组 / 下拉框 → 选择选项
3. 填写完成后点 **"导出"**，填写内容保留在 PDF 中
4. 可选：开启 **"导出时扁平化表单"**，将字段烧录为静态内容（之后不可再编辑）

---

## 九、加密 / 解密

点击左侧 **"加密 / 解密"** 按钮：

### 加密
1. 切到"加密"标签
2. **用户密码**：打开文档需要（必填，≥4 位）
3. **所有者密码**：可选，用于修改权限
4. 权限开关：允许打印 / 复制 / 修改 / 填写表单
5. 点 **"加密并导出"**，生成 `encrypted.pdf`

### 解密
1. 切到"解密"标签
2. 输入文档密码
3. 点 **"解密并导出"**，生成无密码的 `decrypted.pdf`

> 解密由标准 qpdf 引擎处理，密码错误会提示"密码错误，请重试"。

---

## 十、导出与下载

1. 点 **"导出"** 按钮（底部工具栏或侧边栏）
2. 左侧可预览文档（支持翻页、Ctrl+滚轮缩放、拖拽平移）
3. 右侧设置：
   - **格式**：PDF / PNG / JPEG
   - **文件名**：自定义
   - **质量**（图片格式）：10%~100%
   - **缩放**（图片格式）：1x~4x
   - **包含批注**：是否写入标注元素
   - **元数据**：标题、作者、主题、关键词
4. 点 **"导出 PDF"** 下载

> 导出的 PDF 会包含：旋转设置、文字、形状、图片、签名、表单值等所有编辑内容。

---

## 十一、快捷键速查

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` / `Ctrl+Shift+Z` | 重做 |
| `Ctrl+C` | 复制选中元素 |
| `Ctrl+V` | 粘贴 |
| `Delete` / `Backspace` | 删除选中元素 |
| `Ctrl+B` | 展开/收起侧边栏 |
| `Ctrl+` `+` / `Ctrl+-` | 放大 / 缩小 |
| `空格 + 拖拽` | 平移画布 |

---

## 十二、常见问题

**Q: 添加的文字导出后变成了方块/乱码？**
A: 编辑器内置了 GB2312 常用汉字子集字体（约 3500 字 + 常用标点）。超出字库的生僻字可能无法正确嵌入，请使用常用汉字。

**Q: 加密的 PDF 打不开了？**
A: 记住设置的密码。解密时输入用户密码；若设置了所有者密码且忘记，无法恢复。

**Q: 表单字段没出现在面板里？**
A: 仅支持标准 AcroForm 字段。部分 PDF 使用 XFA 动态表单（如某些税务表格），pdf-lib 不支持，可先在 Acrobat 中转换为静态表单再编辑。

**Q: 上传大文件卡顿？**
A: 编辑器在浏览器本地处理，大文件（>50MB）可能占用较多内存。建议使用 ≤20MB 的文档。

**Q: 刷新后编辑内容还在吗？**
A: 小文件（≤3MB）的编辑内容保存在浏览器中，刷新可恢复；大文件需在刷新前手动导出保存。

---

## 十三、文件结构参考

```
E:\PDF editior\Inkoro\
├── lib/                       # 核心逻辑
│   ├── store.ts               # 全局状态（图层/旋转/表单值/历史）
│   ├── pdf-utils.ts           # 导出管线（含中文字体）
│   ├── merge-service.ts       # PDF 合并
│   ├── split-service.ts       # PDF 拆分
│   ├── form-service.ts        # 表单读写
│   └── encrypt-service.ts     # 加密/解密（qpdf WASM）
├── components/editor/         # UI 组件
│   ├── toolbar.tsx            # 底部工具栏
│   ├── merge-dialog.tsx       # 合并对话框
│   ├── split-dialog.tsx       # 拆分对话框
│   ├── form-panel.tsx         # 表单面板
│   └── encrypt-dialog.tsx     # 加密对话框
├── public/
│   ├── fonts/NotoSansSC-subset.ttf   # 中文字体子集
│   ├── pdf.worker.min.mjs            # PDF.js worker（本地化）
│   └── qpdf/                         # qpdf WASM 运行时
└── scripts/                   # 开发/验证脚本
```

---
*手册版本：1.0（2025）*
